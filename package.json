{
  "name": "donner-de-la-voix-static",
  "version": "1.0.0",
  "description": "Site statique Donner de la Voix",
  "scripts": {
    "dev": "live-server --port=3000 --open=/index.html",
    "serve": "npx http-server . -p 3000",
    "start": "live-server --port=3000 --open=/index.html",
    "build": "node scripts/generate-env-config.js"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "live-server": "^1.2.2"
  }
}
